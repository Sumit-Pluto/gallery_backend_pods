# Real-ESRGAN (realesrgan/basicsr/gfpgan) + Whisper (faster-whisper) + YOLO (fallback).
# torch/torchvision are installed in the Dockerfile first (cu121) so pip does not
# resolve a CPU wheel underneath these.
fastapi==0.115.6
uvicorn[standard]==0.34.0
ultralytics==8.3.40
realesrgan==0.3.0
basicsr==1.4.2
gfpgan==1.3.8
faster-whisper==1.0.3
numpy==1.24.4
Pillow==10.4.0
httpx==0.28.1
pydantic==2.10.4
