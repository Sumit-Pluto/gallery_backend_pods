# CPU only — no torch, no CUDA. rembg (bg removal) + ffmpeg denoise (system pkg)
# + PP-OCRv6 ONNX + Groq proxy.
fastapi==0.115.6
uvicorn[standard]==0.34.0
rembg==2.0.59
onnxruntime==1.17.3
# rembg 2.0.59 and opencv both predate the numpy 2 ABI break.
numpy<2
Pillow==10.4.0
httpx==0.28.1
pydantic==2.10.4
# PP-OCRv6 reference pipeline deps (vendored as app/ppocr.py).
opencv-python-headless==4.10.0.84
pyclipper==1.3.0.post5
PyYAML==6.0.2
