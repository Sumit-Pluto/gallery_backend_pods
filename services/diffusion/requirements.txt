# FLUX.2 (Flux2KleinPipeline, still main-branch only) + SDXL inpaint.
# torch cu124 is installed in the Dockerfile first.
#
# Pinned to a COMMIT, not `main`. An unpinned git dep means a rebuild months from
# now installs different code than the pod is running today — that is how a
# working pod becomes a broken one with no commit of yours in between.
#
# This SHA was verified to contain src/diffusers/pipelines/flux2/pipeline_flux2_klein.py
# and to export Flux2KleinPipeline from the package root. To move it: bump the
# SHA, rebuild, and confirm the diffusion pod reaches /readyz before deploying.
diffusers @ git+https://github.com/huggingface/diffusers.git@90b4e34e79a86ec5e7f2437634fe95ecd2108796
transformers>=4.49.0
accelerate>=1.2.0
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=4.25.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
Pillow==10.4.0
httpx==0.28.1
pydantic==2.10.4
